apiVersion: apps/v1
kind: Deployment
metadata:
  name: edu-goods
  namespace: edu-goods
spec:
  replicas: 2
  selector:
    matchLabels:
      app: edu-goods
  template:
    metadata:
      labels:
        app: edu-goods
    spec:
      containers:
      - name: edu-goods
        image: your-docker-registry/edu-goods:latest
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: edu-goods
  namespace: edu-goods
spec:
  selector:
    app: edu-goods
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
